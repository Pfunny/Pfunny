import React from'react';import{createRoot,Root}from'react-dom/client';import ColoringGenerator from'./ColoringGenerator';let root:Root|null=null;function ensureNavigation(){const nav=document.querySelector('.sidebar nav');if(!nav||nav.querySelector('[data-coloring-generator]'))return;const button=document.createElement('button');button.textContent='Ausmalbild-Generator';button.dataset.coloringGenerator='true';button.addEventListener('click',()=>{document.querySelectorAll('.sidebar nav button').forEach(x=>x.classList.remove('active'));button.classList.add('active');const title=document.querySelector('.content > .topbar h1');if(title)title.textContent='Ausmalbild-Generator';const content=document.querySelector('.content');if(!content)return;Array.from(content.children).forEach(x=>{if(!x.classList.contains('topbar'))x.remove()});const panel=document.createElement('section');panel.className='panel';content.appendChild(panel);root?.unmount();root=createRoot(panel);root.render(<ColoringGenerator/>)});nav.appendChild(button)}new MutationObserver(()=>queueMicrotask(ensureNavigation)).observe(document.body,{childList:true,subtree:true});window.addEventListener('DOMContentLoaded',ensureNavigation);ensureNavigation();